# Player profile

Build profile pages from `getPlayerStats`, `winRateBps`, and `analyzeOpponent`.

Keep write actions separate from public profile views so visitors can browse
without connecting a wallet.
