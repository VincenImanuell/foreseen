# Release checklist

Before publishing a new npm version:

- Run `npm run test`.
- Run `npm run build`.
- Review `npm pack --dry-run`.
- Confirm docs and examples match exported APIs.
CELO release checklist: verify DEPLOYMENTS addresses on Celoscan before tagging a new npm version.
