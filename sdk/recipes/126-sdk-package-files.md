# SDK package files

The npm package includes dist, README, AI-readable guides, examples, and
recipes.

It should not include local environment files, caches, or build artifacts outside
the intended dist output.
