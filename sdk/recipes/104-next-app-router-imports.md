# Next App Router Imports

Use package imports that work cleanly in the Next App Router.

Checklist:

- Show the npm package name as `@foreseen/sdk`.
- Avoid browser-only SDK usage in server components.
- Verify imports in both route pages and client components.
- Keep the recipe compatible with a Next.js frontend and a plain npm install.
