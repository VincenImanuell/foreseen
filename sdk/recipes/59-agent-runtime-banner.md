# Agent runtime banner

Print a startup banner with network, wallet address, strategy, and limits.

Do not print private keys, salts, mnemonic phrases, raw environment variables,
or full secret-provider responses.
CELO agent runtime banner should display the CELO address and network (mainnet/testnet).
