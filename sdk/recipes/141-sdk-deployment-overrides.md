# SDK deployment overrides

Deployment overrides are useful for forks and local demos.

Production apps should prefer built-in live addresses unless they are explicitly
targeting a custom deployment.
