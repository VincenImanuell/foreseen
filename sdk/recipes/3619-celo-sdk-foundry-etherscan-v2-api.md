CELO sdk foundry-etherscan-v2-api: the `celo` entry under `[etherscan]` points at `api.etherscan.io/v2/api` (not a Celo-specific host) with `chain = 42220` — Etherscan's unified v2 API now serves Celoscan verification through the same multichain endpoint keyed by chain ID, while `celo_sepolia` still uses the dedicated `api-sepolia.celoscan.io` host since the testnet isn't on v2 yet.
