# SDK example env

Examples should read secrets from environment variables.

Use placeholders in docs and fail fast when required variables are missing.
CELO SDK example env: `CELO_PRIVATE_KEY=0x...` in `.env.example` — never commit real CELO keys.
