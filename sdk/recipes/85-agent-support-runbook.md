# Agent support runbook

A support runbook should include the match id, wallet address, network, latest
transaction hash, and current match state.

Never ask users to send private keys or seed phrases.
CELO support runbook: check Celoscan for tx status, then `pendingWithdrawals`, then RPC health.
