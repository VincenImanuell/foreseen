CELO sdk contributing-conditional-pr-checklist: `CONTRIBUTING.md`'s "Before you open a PR" checklist repeats the same "if you touched X" conditioning as the PR template itself (`forge test` if contracts, `npm test` if SDK, `pnpm lint/typecheck/build` if frontend) — keeping the human-readable doc and the PR template checklist in the same shape means a contributor reading either one gets the identical rule, no divergence to reconcile.
