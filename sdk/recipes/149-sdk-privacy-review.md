# SDK privacy review

Before shipping a dashboard, review what is public on-chain and what is local
runtime data.

Public stats can be displayed. Runtime secrets should never leave the client or
agent environment.
