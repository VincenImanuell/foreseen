# Agent dry run

Before funding an agent, run its strategy against read-only scouting data.

Collect the intended move, confidence label, and reason without submitting a
transaction. This catches bad fallback logic before CELO is at risk.
CELO agent dry run: use Celo Sepolia (chainId 11142220) before committing CELO mainnet gas.
