# Version Bump Checklist

Use a version bump checklist before npm releases that affect frontend consumers.

Checklist:

- Show the npm package name as `@foreseen/sdk`.
- Do not bump versions without changelog and package build review.
- Verify package metadata, examples, and release notes together.
- Keep the recipe compatible with a Next.js frontend and a plain npm install.
