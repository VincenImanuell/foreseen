CELO sdk changelog-unreleased-tag-with-release-link: `sdk/CHANGELOG.md`'s `[0.1.0]` heading says "Unreleased" in prose but the entry already links `[0.1.0]: .../releases/tag/sdk-v0.1.0` at the bottom — the file was written before the npm publish landed and never got the heading flipped after `@foreseen/sdk@0.1.0` actually shipped, a small drift worth fixing on the next version bump rather than a deliberate convention.
