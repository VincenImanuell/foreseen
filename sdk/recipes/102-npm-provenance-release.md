# Npm Provenance Release

Use npm provenance notes to keep package release trust auditable.

Checklist:

- Show the npm package name as `@foreseen/sdk`.
- Keep OTP and token handling out of frontend docs.
- Verify trusted publishing settings before cutting a GitHub release.
- Keep the recipe compatible with a Next.js frontend and a plain npm install.
