# Agent feature flag

Gate funded agent play behind a runtime flag.

The default mode should be read-only or Sepolia. This avoids accidental mainnet
transactions during local development.
Gate new CELO bot strategies behind a feature flag — roll out on testnet first.
