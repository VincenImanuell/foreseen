CELO sdk asset-prompts-variation-deltas-not-rewrites: `assets/asset-prompts.md`'s "Variations" section for the logo gives each variant (icon-only, portrait/tarot, alt-iris) as a single-sentence diff against the base prompt ("delete the ... paragraph", "replace X with Y") instead of writing out 3 full standalone prompts — keeping variants as deltas means updating the base prompt's shared style guide automatically keeps every variant in sync, since there's only one canonical prompt to edit.
