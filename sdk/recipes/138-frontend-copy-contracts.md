# Frontend Copy Contracts

Document frontend copy contracts between SDK return values and UI labels.

Checklist:

- Show the npm package name as `@foreseen/sdk`.
- Do not let package helper names leak into player-facing text.
- Check labels for match mode, stake, result, and deadline fields.
- Keep the recipe compatible with a Next.js frontend and a plain npm install.
CELO frontend copy contracts: reference deployed CELO mainnet addresses in `lib/contracts.ts`, not env.
