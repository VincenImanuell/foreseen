# Dashboard stale state

Mark data stale when a read is older than your refresh interval.

Stale labels are better than silently showing old lobbies or outdated scouting
reads.
Stale CELO state: re-fetch after each CELO block (~5s) to keep match states current.
