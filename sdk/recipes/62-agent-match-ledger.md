# Agent match ledger

Keep a local ledger of match id, opponent, stake, chosen move, confidence, and
transaction hashes.

The ledger is for operations and debugging. It should not contain private keys
or live commit salts.
CELO agent match ledger: log matchId, playerA, bet, result, and CELO tx hashes for each match.
