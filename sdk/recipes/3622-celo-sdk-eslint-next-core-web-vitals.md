CELO sdk eslint-next-core-web-vitals: `frontend/.eslintrc.json` extends only `next/core-web-vitals` — Next.js's stricter preset that upgrades performance-affecting rules (like `<img>` usage or unoptimized `next/image` props) from warnings to errors, on top of its base React/hooks rules, matching a project that ships a public-facing marketing+arena page where load performance is part of the product.
