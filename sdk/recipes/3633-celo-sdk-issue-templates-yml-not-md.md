CELO sdk issue-templates-yml-not-md: both `bug_report.yml` and `feature_request.yml` use GitHub's structured issue-forms YAML format rather than the older Markdown issue-template style — YAML forms render as labeled inputs with per-field validation (`required: true/false`), which blocks an empty submission client-side instead of relying on a maintainer to notice a blank template was submitted.
