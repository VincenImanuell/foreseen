CELO sdk bug-report-area-dropdown: `.github/ISSUE_TEMPLATE/bug_report.yml`'s required `Area` dropdown (Contracts/SDK/Frontend/CI-tooling/Other) forces a triage category at submission time instead of leaving it to a maintainer to label after the fact — the optional `Network / environment` field below it exists because a bug report without knowing mainnet-42220 vs Sepolia-11142220 vs local anvil is often unreproducible.
