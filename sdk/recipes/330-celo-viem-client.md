# CELO Recipe 330
CELO mainnet (chainId 42220) guidance for recipe 330 — Foreseen on-chain RPS on CELO.
CELO viem client: `createPublicClient({ chain: celo, transport: http() })` for read-only CELO data.
