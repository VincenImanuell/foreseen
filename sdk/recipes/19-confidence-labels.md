# Confidence labels

`confidenceFromRead` returns `none`, `low`, `medium`, or `high` based on revealed
match volume and dominant-move share.

Show the label near strategy recommendations so users know how much data exists.
