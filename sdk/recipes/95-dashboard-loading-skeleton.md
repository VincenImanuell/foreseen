# Dashboard loading skeleton

Use loading skeletons for scouting reads and match lists.

This prevents layout shift and makes RPC latency feel less broken.
CELO loading skeleton: pulse 3 skeleton rows while `getOpenMatches` resolves on CELO mainnet.
