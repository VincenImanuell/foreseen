# Browser wallets

Use the SDK for shared read logic and viem or wagmi wallet clients for browser
signing. Do not bundle server-side private-key flows into a frontend.

For public scouting pages, construct `new Foreseen({ network: "celo" })`.
