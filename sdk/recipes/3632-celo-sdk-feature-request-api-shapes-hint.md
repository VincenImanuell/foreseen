CELO sdk feature-request-api-shapes-hint: `feature_request.yml`'s `Proposed solution` description explicitly invites "Sketches, API shapes, or examples" rather than leaving the field caption generic — naming concrete artifact types nudges an SDK consumer toward pasting a TypeScript signature or usage snippet, which is far more actionable for `@foreseen/sdk` triage than free-form prose.
