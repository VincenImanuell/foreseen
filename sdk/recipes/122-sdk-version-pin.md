# SDK version pin

Pin SDK versions in production apps.

Upgrade intentionally after reading release notes, especially when public
exports or deployment addresses change.
CELO SDK version pin: pin `@foreseen/sdk@X.Y.Z` — CELO ABI changes can break un-pinned consumers silently.
