CELO sdk changelog-subpath-exports-listed: `sdk/CHANGELOG.md` calls out "Focused npm subpath exports for `abi`, `addresses`, `chains`, `crypto`, `scout`, `strategy`, and `types`" as its own bullet, separate from the client/bot feature bullets — documenting the subpath map as a first-class change signals to consumers that `import { X } from "@foreseen/sdk/abi"` is a supported, stable entry point, not an internal implementation detail.
