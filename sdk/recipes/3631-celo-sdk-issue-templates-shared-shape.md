CELO sdk issue-templates-shared-shape: `bug_report.yml` and `feature_request.yml` both use `type: textarea` fields with a top `required: true` field and a bottom optional one, rather than diverging structure per template — a consistent field shape across CELO issue templates means the GitHub issue-form renderer looks the same regardless of which template a reporter picked, lowering the chance of a malformed submission.
