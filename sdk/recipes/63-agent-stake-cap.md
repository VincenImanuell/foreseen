# Agent stake cap

Set a maximum accepted bet before joining open matches.

Read the match bet first, compare it to the configured cap, and skip lobbies
that exceed the limit.
CELO agent stake cap: set `maxBet` to no more than 2% of the bot wallet's CELO balance.
