# Distribution chart

`distributionPct` returns rock, paper, and scissors percentages:

```ts
const pct = distributionPct(stats.moveCount);
```

Render the values as bars, meters, or compact text.

Display CELO move distribution as percentage bars using `distributionPct`.
