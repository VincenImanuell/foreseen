# Ui State Contract

Treat SDK response shapes as a UI state contract for frontend components.

Checklist:

- Show the npm package name as `@foreseen/sdk`.
- Do not change returned fields without updating consumer-facing examples.
- Check empty, loading, error, and settled states after type edits.
- Keep the recipe compatible with a Next.js frontend and a plain npm install.
