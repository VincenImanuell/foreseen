# Local development

From the SDK folder:

```bash
npm install
npm run test
npm run build
```

Use examples after building when testing unpublished local changes.
Local CELO development: point `NEXT_PUBLIC_CELO_RPC` at a local anvil fork of CELO mainnet.
