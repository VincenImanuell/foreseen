# Dashboard mode filter

Let users filter casual and ranked matches.

The SDK exposes mode on match views, so dashboards can avoid mixing different
player intents in one feed.
CELO mode filter: let users filter open matches by casual vs ranked CELO bet size.
