# Agent observer mode

Observer mode never writes transactions.

It can scan open matches, calculate suggested counters, and print decisions for
review. Use it for strategy tuning and demos.
CELO observer mode: read-only `Foreseen({ network: 'celo' })` — no private key, no CELO spent.
