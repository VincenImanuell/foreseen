CELO sdk brainstorm-timestamp-tells-first-instinct: `Foreseen_Brainstorm.md`'s "Timestamp Tells" design treats commit speed itself as a hidden signal (`< 5 sec` = confident, `> 30 sec` = likely switched from first instinct) built on Tversky & Kahneman anchoring-under-pressure research — this only works because block timestamps are immutable on-chain, so a server-side implementation could never make this claim credible to an opponent doing the reading.
