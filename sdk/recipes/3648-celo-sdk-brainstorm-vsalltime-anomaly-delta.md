CELO sdk brainstorm-vsalltime-anomaly-delta: `Foreseen_Brainstorm.md`'s `analyzeOpponent(address, { timeRange })` design returns a `vsAllTime` delta (e.g. `rockDelta: -30`) alongside the raw percentages, not just the timeframe slice on its own — surfacing the deviation from baseline is what turns "70% scissors today" into an actionable "suspicious anomaly" flag instead of a number the caller has to mentally diff themselves.
