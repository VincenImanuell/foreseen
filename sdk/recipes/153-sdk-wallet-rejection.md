# SDK wallet rejection

Wallet rejection is a normal user choice.

Surface it as a cancelled action, not as a scary system failure. Do not retry
automatically after a rejection.
CELO wallet rejection: if user rejects CELO tx, show 'Cancelled in wallet — no CELO was spent.'
