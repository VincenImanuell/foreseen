# Dashboard refresh button

Auto-refresh is useful, but keep a manual refresh button.

Manual refresh gives users control when they are waiting for a transaction to
appear or a deadline to update.
CELO refresh button: re-fetch CELO match list with `getOpenMatches` on click, not polling.
