# Typed Network Switch

Use typed network helpers so frontend SDK consumers do not string-match chain names.

Checklist:

- Show the npm package name as `@foreseen/sdk`.
- Do not duplicate deployment addresses across examples.
- Check TypeScript output after editing network exports.
- Keep the recipe compatible with a Next.js frontend and a plain npm install.
