# Mode selection

`mode` accepts `"casual"`, `"ranked"`, or the `Mode` enum.

Use casual mode for experiments. Use ranked mode when the user explicitly opts
into rank progression.

Both casual and ranked modes run on CELO mainnet (chainId 42220) — same escrow, different badge logic.
