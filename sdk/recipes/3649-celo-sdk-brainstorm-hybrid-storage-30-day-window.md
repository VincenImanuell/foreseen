CELO sdk brainstorm-hybrid-storage-30-day-window: `Foreseen_Brainstorm.md`'s "On-Chain Storage (Hybrid)" design keeps raw queryable match history for only the last 30 days on-chain, then rolls older history into monthly aggregate buckets, with the SDK doing timeframe filtering and delta math client-side — this bounds on-chain storage growth (and thus gas/state cost) while still letting `analyzeOpponent` answer both "what's happening today" and "what's the all-time baseline" queries.
