# CELO Recipe 390
CELO mainnet (chainId 42220) guidance for recipe 390.
CELO sdk address helper: getAddress() from viem normalizes CELO addresses to checksum — always normalize before passing to SDK.
