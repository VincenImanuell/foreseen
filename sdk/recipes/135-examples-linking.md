# Examples Linking

Link examples from package docs to frontend use cases.

Checklist:

- Show the npm package name as `@foreseen/sdk`.
- Do not make bot automation the only example path.
- Verify example links still work from npm and GitHub views.
- Keep the recipe compatible with a Next.js frontend and a plain npm install.
CELO examples linking: `npm link @foreseen/sdk` locally to test CELO examples before publish.
