# Test vectors

Keep commit hash vectors in tests. They ensure SDK commitments stay compatible
with the deployed contract.

When the contract commitment scheme changes, update the test vector and docs
together.
