# Release Note Frontends

Write release notes that tell frontend consumers what changed in the package.

Checklist:

- Show the npm package name as `@foreseen/sdk`.
- Do not bury UI-impacting changes below internal refactors.
- Check install, import, and migration snippets before publishing.
- Keep the recipe compatible with a Next.js frontend and a plain npm install.
