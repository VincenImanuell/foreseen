# Webhook indexers

If an app needs instant feeds, use an indexer or webhook service to watch match
events, then call the SDK for authoritative reads before rendering.

The SDK does not require an indexer for basic usage.
