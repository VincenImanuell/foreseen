<!-- Keep PRs focused and small. One concern per PR. -->

## What

<!-- Summary of the change in one or two sentences. -->

## Why

<!-- The motivation / issue this addresses. Link issues with "Closes #N". -->

## How

<!-- Notable implementation details, trade-offs, or follow-ups. -->

## Checklist

- [ ] `forge test` passes (if contracts changed)
- [ ] `pnpm lint` and `pnpm build` pass in `frontend/` (if frontend changed)
- [ ] `npm test` passes in `sdk/` (if SDK changed)
- [ ] No secrets, private keys, or `.env` values committed
- [ ] Scope is limited to a single concern
