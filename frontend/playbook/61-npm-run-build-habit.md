# Npm Run Build Habit

Make `npm run build` the standard frontend verification command after UI or package changes.

Checklist:

- Keep the note tied to the Foreseen game frontend and the active Celo network.
- Do not substitute dev-server success for a production build check.
- Capture warnings that matter for Next, wagmi, viem, or SDK imports.
- Leave secrets, generated builds, and local caches out of Git.
