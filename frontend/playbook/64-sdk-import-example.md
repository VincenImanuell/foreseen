# Sdk Import Example

Show SDK imports in frontend docs using the public package entrypoint.

Checklist:

- Keep the note tied to the Foreseen game frontend and the active Celo network.
- Avoid relative imports into SDK source from the frontend app.
- Run TypeScript after changing import examples that builders may paste.
- Leave secrets, generated builds, and local caches out of Git.
