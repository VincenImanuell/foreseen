# Local Dev Network Note

Clarify which Celo network the local frontend points at before a tester opens the arena.

Checklist:

- Keep the note tied to the Foreseen game frontend and the active Celo network.
- Keep mainnet and Sepolia labels explicit near setup notes.
- Verify public env defaults with a fresh `npm run dev` session.
- Leave secrets, generated builds, and local caches out of Git.
