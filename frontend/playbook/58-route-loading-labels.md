# Route Loading Labels

Make route-level loading labels match the data source the frontend is waiting on.

Checklist:

- Keep the note tied to the Foreseen game frontend and the active Celo network.
- Do not call a read-only SDK query a transaction.
- Verify labels still fit in compact cards on mobile.
- Leave secrets, generated builds, and local caches out of Git.
