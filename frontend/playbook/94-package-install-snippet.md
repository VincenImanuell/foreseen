# Package Install Snippet

Keep the package install snippet short and scoped to the consuming frontend.

Checklist:

- Keep the note tied to the Foreseen game frontend and the active Celo network.
- Do not include registry tokens in install examples.
- Verify the snippet works from a clean app directory.
- Leave secrets, generated builds, and local caches out of Git.
