# Package Manager Boundary

Make the active package-manager expectation clear when contributors move between npm and pnpm artifacts.

Checklist:

- Keep the note tied to the Foreseen game frontend and the active Celo network.
- Do not mix lockfile instructions inside the same quick-start block.
- Check that generated package caches stay ignored before committing docs or UI files.
- Leave secrets, generated builds, and local caches out of Git.
