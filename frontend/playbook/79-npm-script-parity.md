# Npm Script Parity

Keep documented npm scripts aligned with `package.json` so frontend setup stays copy-pasteable.

Checklist:

- Keep the note tied to the Foreseen game frontend and the active Celo network.
- Do not invent scripts in docs before they exist in the package.
- Verify `dev`, `build`, and `start` descriptions after script edits.
- Leave secrets, generated builds, and local caches out of Git.
