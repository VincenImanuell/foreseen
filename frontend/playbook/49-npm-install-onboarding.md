# Npm Install Onboarding

Keep first-run setup short so a new builder can install the frontend before touching wallet flows.

Checklist:

- Keep the note tied to the Foreseen game frontend and the active Celo network.
- Show the install command beside the frontend directory, not buried below gameplay notes.
- Verify the next step with `npm run build` before pointing the builder at deploy previews.
- Leave secrets, generated builds, and local caches out of Git.
