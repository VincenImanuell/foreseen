# Typed Env Labels

Keep public environment labels typed and readable wherever the frontend names a network.

Checklist:

- Keep the note tied to the Foreseen game frontend and the active Celo network.
- Do not place private keys or deployer secrets in frontend env examples.
- Check both `.env.example` and route copy when new public variables appear.
- Leave secrets, generated builds, and local caches out of Git.
